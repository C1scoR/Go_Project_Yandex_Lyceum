## 📝 **Краткое описание**
Этот проект - работа за 3-й спринт Яндекс Лицея. Он представляет из себя суперпупермега калькулятор, который содержит 3 микросервиса `однако это больше соответсвует SOA архитекутре чем микросервисной, так как все 3 микросервиса работают от одной базы данных`. 
#### **Микросервисы**:
1) Агент - посылает по gRPC запросы на оркестратор для получения выражений (Server-Streaming)
2) Оркестратор1 - отдаёт выражения агенту по gRPC, если таковые имеются (Polling)
3) Оркестратор2 - служит для взаимодействия с пользователем

## 🔧**Технологии**
[The Go Programming Language](https://go.dev/)

[Postman](https://www.postman.com/)

[gRPC](https://grpc.io/docs/languages/go/basics/)

[sqlLite](https://www.sqlite.org/)
## 🚀**Как запустить проект**
По шагам:
1) Открываете VS Code/Goland
2) Добавляете .env файл в директорию `internal/orchestrator`
3) Настраиваете зависимости в .env файле:
Пример приведён ниже:
```.env
TIME_ADDITION_MS =  #время выполнения операции сложения в миллисекундах
TIME_SUBTRACTION_MS =  #время выполнения операции вычитания в миллисекундах
TIME_MULTIPLICATIONS_MS =  # время выполнения операции умножения в миллисекундах
TIME_DIVISIONS_MS =  #время выполнения операции деления в миллисекундах
PORT_ORCHESTRATOR =  # Порт на котором запускается оркестратор
JWT_SECRET =  #указывается секретная строка для подписи токена
TOKEN_EXPIRY =  #указывается через сколько исткекает токен в часах
REFRESH_EXPIRY =  #указывается через сколько обновляется токен в часах
```
4) Далее вам нужно создать 2 терминала. В VS code - это сочетание клавиш `Ctrl+Shift+5`, в Goland придётся просто открыть 2 вкладки терминала: `View` → `Tool Windows` → `Terminal` (или нажмите `Alt + F12`).
5) Переходите в директории, где лежат main файлы для агента и для оркестратора:
   `cd .\cmd\agent` и `cd .\cmd\orchestrator`
6) Далее компилируете main файлы и запускаете их в разных терминалах соответственно
   Команды для запуска main:
   - `go build main.go`
   - `go run main.go`
7) Готово, если у вас всё получилось, раз в несколько секунд вы увидите логи **Агента** о выполнении запросов, а **Оркестратор** покажет на каком порту запустились его 2 микросервиса. Один микросервис общается по `gRPC` с агентом, второй же обрабатывает запросы пользователя.
   - **Это сообщение вы увидите после запуска:**
 <img width="859" alt="image" src="https://github.com/user-attachments/assets/e0b8d293-3115-4994-bd9c-ce34b7b91b52" />


## ⚡**Использование**
После запуска **Агента** и **Оркестратора** вы наконец-то можете делать запросы на сервер.

### **Регистрация**
Для начала использования сервиса необходимо пройти регистрацию. Таким образом система будет знать, что такой пользователь есть в её базе данных.
Создайте запрос в удобном вам формате: (**Советую перейти на postman, так как он запоминает запросы, и не нужно какждый раз это переписывать**)
#### Постман:
<img width="255" alt="image" src="https://github.com/user-attachments/assets/0416a8f9-4e76-413d-bcfe-9d39023bde5d" />

#### Curl:
```c
curl --location 'http://localhost:<ВАШ_ПОРТ>/api/v1/register' \
--header 'Content-Type: application/json' \
--data '{
  "email": "<ВАШ_EMAIL>",
  "password": "<ВАШ_ПАРОЛЬ>"
}'
```
### Примеры ответов:
### Если запрос был выполнен успешно, то вы получите соответствующее сообщение:

<img width="308" alt="image" src="https://github.com/user-attachments/assets/bd0ce230-e85c-442b-b203-b2f7147e106e" />

### Если запрос не удался, то выведется сообщение об ошибке, например:

<img width="376" alt="image" src="https://github.com/user-attachments/assets/c8dbafe5-15fc-47f2-b214-5ba30128700c" />

### Если пользователь уже зарегистрирован, то выведется соответствующее сообщение:

<img width="274" alt="image" src="https://github.com/user-attachments/assets/47a18040-09ae-4cc6-ab43-944bc07837d1" />

### **Логин**
Чтобы выполнять запросы на любые handlerы необходимо войти в систему, получить свой личный `jwt-токен` и отправлять его дальше в заголовках запроса.
Создадим запросы в удобном для нас формате:
#### Постман:
<img width="520" alt="image" src="https://github.com/user-attachments/assets/43feb880-61d9-4e16-80a2-4b1aa8af63be" />

#### Curl:
```c
curl --location 'http://localhost:<ВАШ_ПОРТ>/api/v1/login' \
--header 'Content-Type: application/json' \
--data '{
  "email": "<ВАШ_EMAIL>",
  "password": "<ВАШ_ПАРОЛЬ>"
}'
```
### Примеры ответов:
### Успешный запрос:

<img width="360" alt="image" src="https://github.com/user-attachments/assets/52caee19-e9d2-47b6-ba63-497d6dbb58de" />

### Запрос с ошибкой:

<img width="331" alt="image" src="https://github.com/user-attachments/assets/574f7b18-258e-4a15-9291-3c3815780498" />

### Пользователь не был добавлен в Базу Данных:

<img width="403" alt="image" src="https://github.com/user-attachments/assets/aa806025-2af1-4cd9-932c-a2c074c4430d" />

### Запрос с неверным паролем:

<img width="327" alt="image" src="https://github.com/user-attachments/assets/674646c3-21f2-4ede-b5b9-c6375fd495fc" />

- #### Вы также можете запустить `интеграционные тесты`, которые находятся в пакете `database/handlers/`, чтобы проверить всё что я описал выше.

## 🤝 **Дальнейшее взаимодействие**
Этот пункт я решил выделить из-за его важности для проведения дальнейшних запросов.
- Полученные после `login` токены необходимо сохранить (в тг Saved Messages закиньте 😁)и передавать в каждом запросе, так как передо мной не стояло задачи реализовать **cookie** для сохранения токенов.
- Чтобы использовать токен в `Postman` необходимо передавать его в соответствующей вкладке, на каринке ниже представлено где именно:
<img width="374" alt="image" src="https://github.com/user-attachments/assets/342c8a9c-3cd0-4f56-841b-75cf04ca786b" />

### **Добавление выражения**
Выражение добавляется в хранилище выражений `Expressions_storage`, которые в порядке очереди будут поступать в **Оркестратор**.
Для добавления выражения создайте запрос:
```c
curl --location 'http://localhost:<ВАШ_ПОРТ>/api/v1/calculate' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer <ВАШ_ТОКЕН>' \
--data '{
  "expression": "<ВАШЕ_ВЫРАЖЕНИЕ>"
}'
```
- Обязательно после `localhost` - укажите значение порта, на котором у вас запускается сервер. Это значение указывается в `.env` файле. 
- В expression добавьте строку выражения в формате `"x+y*z..."`.
- Добавьте ваш токен в заголовок авторизации.
### Пример:
<img width="524" alt="image" src="https://github.com/user-attachments/assets/b4b11483-ef58-4daa-a490-401ca59d9f68" />

Сверху - выражение, снизу ответ **Оркестратора**
### Пример с ошибкой:

<img width="531" alt="image" src="https://github.com/user-attachments/assets/aaf4bac8-51c7-4084-bf67-715025137941" />

### Пример с инвалидным токеном:
<img width="357" alt="image" src="https://github.com/user-attachments/assets/8fcd98a5-a2e9-455a-9a27-33945fa4dd47" />

### Пример без передачи токена:
<img width="412" alt="image" src="https://github.com/user-attachments/assets/d7143470-cf54-4f90-9892-8f3d8fa7fc32" />

- #### Для остальных защищённых обработчиков при передачи ивналидного токена или без его передачи будут те же самые ошибки 
### **Получение списка выражений**
```c
curl -X GET http://localhost:<ВАШ_ПОРТ>/api/v1/expressions' \
  -H "Authorization: Bearer <ВАШ_ТОКЕН>"
```
- Обязательно после `localhost` - укажите значение порта, на котором у вас запускается сервер. Это значение указывается в `.env` файле. 
### Пример:

<img width="414" alt="image" src="https://github.com/user-attachments/assets/795f64eb-59e3-4d3f-812e-30fdc474a1a2" />

Сверху - выражение, снизу ответ **Оркестратора**

### Пример с ошибкой

<img width="579" alt="image" src="https://github.com/user-attachments/assets/9f6d1f8d-94e9-46da-b2c0-e1fab1d5141d" />

### **Получение выражения по его идентификатору**
```c
curl --location 'localhost:<ВАШ_ПОРТ>/api/v1/expressions/:id' \
-H "Authorization: Bearer <ВАШ_ТОКЕН>"
```
- Обязательно после `localhost` - укажите значение порта, на котором у вас запускается сервер. Это значение указывается в `.env` файле. 
- Поле `:id` в запросе поменяйте на именование id выражения, которое вы хотите достать
### Пример: 
<img width="407" alt="image" src="https://github.com/user-attachments/assets/a82d7fc1-96a6-441b-92ad-4b105389c81d" />

### Пример с ошибкой:
<img width="432" alt="image" src="https://github.com/user-attachments/assets/f6428ab1-6c77-4b4c-81e5-e7a2c1ccd3da" />

### **Получение данных профиля пользователя**
```c
curl --location 'localhost:<ВАШ_ПОРТ>/api/v1/profile' \
-H "Authorization: Bearer <ВАШ_ТОКЕН>"
```
### Пример:
<img width="384" alt="image" src="https://github.com/user-attachments/assets/f19488e8-7745-4921-97cd-852fa5ad39ae" />

- Ошибиться в этом запросе можно только в передаче токена, поэтому не вижу смысла прикреплять пример с ошибкой.

### **Запрос на обновление токена**
```c
curl --location 'localhost:<ВАШ_ПОРТ>/api/v1/refresh-token' \
-H "Authorization: Bearer <ВАШ_ТОКЕН>"
```
### Пример:
<img width="357" alt="image" src="https://github.com/user-attachments/assets/9a3a50c5-440d-4564-a570-30e5ffb4d440" />

- Ошибиться в этом запросе можно только в передаче токена, поэтому не вижу смысла прикреплять пример с ошибкой.

## 🤔 **Как это всё работает**

### **Регистрация** 
- Пользователь делает запрос, отправляя свои `email` и `password`
- Они парсятся и проходят проверку на валидность
- Далее проверяется есть ли уже такой пользователь в базе данных
- Затем хэшируется пароль
- Открывается транзакция на вставку данных в БД
- Если всё успешно, то транзакция закрывается и выводится сообщение об успешной регистрации

Реализацию можете глянуть в `internal/database/handlers/handlers.go`.
Сигнатура:
```go
func (h *AuthHandler) Register(w http.ResponseWriter, r *http.Request) 
```

### **Логин**
- Пользователь делает запрос, отправляя свои `email` и `password`
- Они парсятся и проходят проверку на валидность
- Далее проверяется есть ли уже такой пользователь в базе данных
- Пароль сверяется с его захешированной версией
- Если всё удачно, то создаётся `jwt` токен, который отправляется пользователю

Реализацию можете глянуть в `internal/database/handlers/handlers.go`.
Сигнатура:
```go
func (h *AuthHandler) Login(w http.ResponseWriter, r *http.Request) 
```

### **Добавление выражения работает примерно так:**
- Пользователь делает запрос
- Middleware проверяет присутствует ли токен, если это защищённая ручка.
- Middleware разбирает этот токен, после чего в контексте передаёт всем защищённым ручкам идентификатор пользователя и его email.
- Выражение парсится из этого запроса в данные, которые можно запихнуть в структуру
- Ну и соответственно эта структура - массивчик, из которого мы по мере необходимости будем отдавать выражения на развыраженивание (разбиение на параллельные задачи), а затем эти подзадачки будем отдавать агенту, но об этом позже.

Вот так выглядит массив в который парсятся выражения
```go
type Expressions_parametres struct {
    ID     string `json:"id"`
    Status string `json:"status"`
    Result string `json:"result"`
}

  

type Expressions_storage struct {
    Expressions []Expressions_parametres `json:"expressions"`
}
// массивчик с выражениями от пользователя
var Expressions_storage_variable Expressions_storage
```
- Это выражение добавляется в базу данных для конкретного пользователя (повторюсь, id взяли из контекста)
- Выражение получает свой уникальный идентификатор, который создаётся при помощи `uuid`
- Ставится статус `StatusCreated`
- В `Result` пока записывается просто само выражение, а не его результат

Реализацию можете глянуть в `\internal\orhestrator\orchestrator.go`, сигнатура handlerа:
```go
func OrchestratorHandler(w http.ResponseWriter, r *http.Request)
```
### **Запрос на все выражения:** 
- Пользователь делает запрос
- Middleware проверяет есть ли токен и валиден ли он
- Middleware парсит из `jwt`  и передаёт `id` пользователя в обработчик по адресу `localhost:<ВАШ_АДРЕС>/api/v1/expressions`
- Далее делается запрос в базу данных на получение выражений пользователя.
- Отдаём пользователю все выражения с их `id`, уравнением и результатами

Реализацию можете глянуть в `\internal\orhestrator\orchestrator.go`, сигнатура handlerа:
```go
func GetExpressionsHandler(w http.ResponseWriter, r *http.Request) 
```
Маленькая поправка:
Если выражение посчиталось, то будет записан его результат, если выражение не посчиталось, то будет записано само выражение.

### **Получение выражения по его идентификатору**
- Пользователь делает запрос
- Middleware проверяет есть ли токен и валиден ли он
- Middleware парсит из `jwt` и передаёт `id` пользователя в обработчик по адресу `localhost:<ВАШ_АДРЕС>/api/v1/expressions/`
- Далее выполняется запрос в базу данных, используя `id` выражения и `id` пользователя
- Получаем выражение
- Парсим это выражение в json
- Отдаём пользователю это выражение

Реализацию можете глянуть в `\internal\orchestrator\orchestrator.go`, сигнатура handlerа:
```go
func GetExpressionByIdHandler(w http.ResponseWriter, r *http.Request)
```

### **Получение профиля пользователя**
- Пользователь делает запрос
- Middleware проверяет есть ли токен и валиден ли он
- Middleware парсит из `jwt` и передаёт `id` и `email` пользователя в обработчик по адресу `localhost:<ВАШ_АДРЕС>/api/v1/profile`
- Из контекста достаются `id` и `email`, собираются в json
- json отправляется пользователю

Реализацию можете глянуть в `internal/database/handlers/handlers.go`.
Сигнатура:
```go
func (h *AuthHandler) GetUserData(w http.ResponseWriter, r *http.Request) 
```

## **Общение агента и оркестратора**
Вот мы и подошли к самой сложной части реализации кода :)
Общение **Агента** и **Оркестратора** происходит по http запросам. **Агент** делает запрос, получает данные, вычисляет, отдаёт **Оркестратору**. Соответственно для **Оркестратора** наоборот: он получает запрос, готовит данные, отдаёт их, и получает результат от **Агента**.

И всё бы было просто, но вот только в синхронной программе. Используя асинхронность и параллельность сложность реализации такой структуры растёт с геометрической прогрессией.

И если говорить понятно и в 2-х словах, то это работает так:
- **Разбор выражения в AST** (парсер).
- **Рекурсивный обход AST**:
    - Атомарные выражения отправляются через канал в очередь.
- **Обработка запроса на Оркестраторе**:
    - Очередь хранит узлы (Node).
    - По запросу **Агента** **Оркестратор** отдаёт накопленные выражения.
- **Обработка выражений на Агенте**:
    - Создаёт пул воркеров.
    - Воркеры получают задания, считают и отправляют **POST** на в **Оркестратор**.
- **Обновление дерева в Оркестраторе:
    - Вставка вычисленных значений по id.
    - Проверка, схлопнулось является ли корневой узел числом.
- **Финальный результат** – когда остался один узел, **Оркестратор** возвращает ответ и записывает result в массив expressions.

Код я жесточайше накостылил различными ограничениями, чтобы хоть как-то оптимизировать работу по памяти, потому что идея о том, что можно 1 раз разобрать дерево для каждого выражения, а потом после вычисления подвыражений просто схлопывать его, когда Node вместо оператора станет операндом (Она пришла мне 5-го марта, то есть сегодня).
А так у меня паралелльность сохраняется, я нигде не обманул, только вот, чтобы вставить Node с операндом на место оператора мне нужно каждый раз заново его разворачивать). А чтобы дерево лишний раз не переобходить, то я на вычисленные узлы ставлю StatusRestrict - и соответственно дерево уже не смотрит на то, чтобы заново выполнять проверку этого выражения на то, что можно его взять или нет.

Ладно, на самом деле это был единственный костыль (Кто не понял - это Node со статусом, если StatusFree - то мы можем её рассмотреть для того, чтобы взять, если StatusRestrict - мы не берём её, потому что либо она сейчас считается, либо уже посчиталась).

#### **Блок схема работы**
<img width="1230" alt="image" src="https://github.com/user-attachments/assets/5a4be789-4370-4fac-87dd-121094066d2f" />


### **Логика работы на примере:**
1) Отправляем Post-запрос
<img width="580" alt="image" src="https://github.com/user-attachments/assets/cf2407ba-96b6-4dd7-9b05-4c93bbb562ad" />
2) Выражение разбивается <img width="516" alt="image" src="https://github.com/user-attachments/assets/777ca07b-a361-4eb6-874b-5f275576d75f" />
3) Выражение подсчитывается <img width="526" alt="image" src="https://github.com/user-attachments/assets/d4c527b6-2e2e-41f8-81a1-1841f0ad17ff" />

# **Прошу обратить внимание**
Посмотрите на логи агента (Под пунктом 3) Агенту поступило 2 выражения, которые могут рассчитаться паралелльно, их результат: 24,5 и 0,92.
Далее мы схлопнули дерево, и отдали задачу которая в дереве стоит на уровень выше: её результат 44,92
И то же самое для 66,42

## **А теперь давайте жёстко затестим параллельность**

<img width="535" alt="image" src="https://github.com/user-attachments/assets/a7e245a7-c636-4af4-b470-241d1fbb767e" />
<img width="538" alt="image" src="https://github.com/user-attachments/assets/d77c3aea-dce9-4a97-92f2-b1037701f17e" />

Можете проверить на калькуляторе). 

- Далее делаем Get-запрос и получаем все наши подсчитанные выражения <img width="509" alt="image" src="https://github.com/user-attachments/assets/476e10ec-9169-4cfb-a071-caef58a9248c" />


# **Спасибо за внимание**
Вот вам лысый Роналдо:
<img width="217" alt="image" src="https://github.com/user-attachments/assets/86485c28-477c-4b8c-83d3-5d3be4ce6e97" />

